import React from 'react'
import ReactDOM from 'react-dom/client'
import '@css/styles.scss'

import App from '@components/AppSample'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
